{
  "name": "team-profile-generator",
  "version": "1.0.0",
  "description": "team generator to build a Node.js command-line application that takes in information about employees on a software engineering team then generates an HTML webpage that displays summaries for each person. so you’ll also write a unit test for every part of your code and ensure that it passes each test.",
  "main": "server.js",
  "scripts": {
    "test": "jest",
    "start": "node server.js"
  },
  "repository": {
    "type": "git",
    "url": "DRockmore89"
  },
  "author": "Donald Rockmore",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "fs": "*",
    "inquirer": "^8.0.0",
    "node": "^16.0.0"
  }
}
